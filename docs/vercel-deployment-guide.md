# Vercel 部署指南

本文档提供将 Admin Console UI 项目部署到 Vercel 的完整指南。

## 📋 项目部署兼容性分析

### ✅ 项目适合 Vercel 部署

| 检查项 | 状态 | 说明 |
|--------|------|------|
| **Next.js 框架** | ✅ 兼容 | 使用 Next.js 16.0.10，Vercel 原生支持 |
| **构建脚本** | ✅ 标准 | `next build` 标准构建命令 |
| **App Router** | ✅ 支持 | 使用 Next.js App Router 架构 |
| **Middleware** | ✅ 兼容 | Edge Runtime 中间件，Vercel 完美支持 |
| **图片优化** | ⚠️ 已禁用 | `images.unoptimized: true`，可在 Vercel 启用 |
| **TypeScript 错误** | ⚠️ 忽略 | `ignoreBuildErrors: true`，建议生产环境修复 |
| **Supabase 集成** | ✅ 兼容 | 使用 `@supabase/ssr`，适合 serverless 环境 |
| **Vercel Analytics** | ✅ 已集成 | 已安装 `@vercel/analytics` |

### 当前 Next.js 配置

```javascript
// next.config.mjs
const nextConfig = {
  typescript: {
    ignoreBuildErrors: true,  // ⚠️ 建议逐步修复 TypeScript 错误
  },
  images: {
    unoptimized: true,  // 可选：在 Vercel 上可启用优化
  },
}
```

---

## 🔐 环境变量配置清单

在 Vercel 项目设置中配置以下环境变量：

### 必需的环境变量

| 变量名 | 描述 | 类型 | 示例值格式 |
|--------|------|------|-----------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 项目 URL | 公开 | `https://xxx.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase 匿名公钥 | 公开 | JWT 格式字符串 |

### 可选的环境变量

| 变量名 | 描述 | 类型 | 默认值 |
|--------|------|------|--------|
| `NEXT_PUBLIC_DEV_MOCK_AUTH` | 开发模式 Mock 认证 | 公开 | `false` |
| `NEXT_PUBLIC_ADMIN_EMAILS` | 管理员邮箱列表（逗号分隔） | 公开 | - |
| `NEXT_PUBLIC_EDITOR_EMAILS` | 编辑者邮箱列表（逗号分隔） | 公开 | - |
| `NEXT_PUBLIC_VIEWER_EMAILS` | 查看者邮箱列表（逗号分隔） | 公开 | - |

### ⚠️ 不应部署的变量

以下变量仅用于本地开发，**不应**在 Vercel 中配置：

- `NEXT_PUBLIC_ADMIN_EMAIL` - 本地测试邮箱
- `NEXT_PUBLIC_ADMIN_PASSWORD` - 本地测试密码
- `GITHUB_EMAIL` - GitHub OAuth 测试凭证
- `GITHUB_PASSWORD` - GitHub OAuth 测试密码

> **安全提示**：生产环境应使用 Supabase Dashboard 管理用户，而不是硬编码凭证。

---

## 🚀 GitHub 连接与自动部署

### 步骤 1：准备 GitHub 仓库

1. 确保代码已推送到 GitHub 仓库
2. 确认 `.gitignore` 包含 `.env.local`（已配置）

### 步骤 2：导入项目到 Vercel

1. 访问 [Vercel Dashboard](https://vercel.com/dashboard)
2. 点击 **Add New...** → **Project**

   ```
   ┌─────────────────────────────────────────┐
   │  Vercel Dashboard                       │
   │  ┌──────────────┐                       │
   │  │ Add New... ▼ │  ← 点击这里           │
   │  └──────────────┘                       │
   │    • Project      ← 选择 Project        │
   │    • Domain                             │
   │    • Storage                            │
   └─────────────────────────────────────────┘
   ```

3. 在 **Import Git Repository** 页面：
   - 选择 **GitHub** 作为 Git 提供商
   - 如果首次使用，需要授权 Vercel 访问 GitHub

   ```
   ┌─────────────────────────────────────────┐
   │  Import Git Repository                  │
   │                                         │
   │  ┌─────────────────────────────────┐   │
   │  │ 🔗 GitHub        [Authorize] │   │
   │  └─────────────────────────────────┘   │
   │                                         │
   │  Select a repository to import:         │
   │  ┌─────────────────────────────────┐   │
   │  │ 🔍 Search...                    │   │
   │  ├─────────────────────────────────┤   │
   │  │ ○ your-org/admin-console-ui  ← │   │
   │  │ ○ your-org/other-repo           │   │
   │  └─────────────────────────────────┘   │
   └─────────────────────────────────────────┘
   ```

4. 找到并选择 `admin-console-ui` 仓库，点击 **Import**

### 步骤 3：配置项目设置

在 **Configure Project** 页面：

```
┌───────────────────────────────────────────────┐
│  Configure Project                            │
│                                               │
│  Project Name: admin-console-ui               │
│  Framework Preset: Next.js  ✓ (自动检测)       │
│  Root Directory: ./                           │
│                                               │
│  Build & Output Settings:                     │
│  ┌───────────────────────────────────────┐   │
│  │ Build Command:   next build           │   │
│  │ Output Dir:      .next                │   │
│  │ Install Command: npm install          │   │
│  └───────────────────────────────────────┘   │
│                                               │
│  Environment Variables:  ← 展开此部分          │
│  ┌───────────────────────────────────────┐   │
│  │ + Add Variable                        │   │
│  └───────────────────────────────────────┘   │
└───────────────────────────────────────────────┘
```

#### 构建设置建议

| 设置项 | 推荐值 | 说明 |
|--------|--------|------|
| **Framework Preset** | Next.js | Vercel 自动检测 |
| **Build Command** | `next build` | 默认即可 |
| **Output Directory** | `.next` | 默认即可 |
| **Install Command** | `npm install` 或 `pnpm install` | 根据锁文件自动选择 |
| **Node.js Version** | 18.x 或 20.x | 推荐 20.x |

### 步骤 4：添加环境变量

1. 展开 **Environment Variables** 部分
2. 逐个添加必需的环境变量：

```
┌───────────────────────────────────────────────┐
│  Environment Variables                        │
│                                               │
│  NAME                          VALUE          │
│  ┌────────────────────────┐  ┌────────────┐  │
│  │ NEXT_PUBLIC_SUPABASE_  │  │ https://...│  │
│  │ URL                    │  │            │  │
│  └────────────────────────┘  └────────────┘  │
│                                               │
│  Environments: ☑ Production ☑ Preview ☐ Dev  │
│                                               │
│  [+ Add Another]                              │
└───────────────────────────────────────────────┘
```

3. 对于每个变量，选择应用的环境：
   - **Production**：正式生产环境
   - **Preview**：PR 预览部署
   - **Development**：Vercel CLI 本地开发

### 步骤 5：部署

1. 点击 **Deploy** 按钮
2. Vercel 将自动：
   - 克隆仓库
   - 安装依赖
   - 执行构建
   - 部署到边缘网络

```
┌───────────────────────────────────────────────┐
│  Deployment                                   │
│                                               │
│  Building...                                  │
│  ├─ Installing dependencies    ✓              │
│  ├─ Building application       ✓              │
│  ├─ Deploying to edge network  ✓              │
│                                               │
│  🎉 Ready!                                    │
│  https://admin-console-ui.vercel.app          │
└───────────────────────────────────────────────┘
```

---

## 🔄 自动部署配置

连接 GitHub 后，Vercel 自动配置：

| 触发条件 | 部署类型 | URL 格式 |
|----------|----------|----------|
| 推送到 `main` 分支 | Production | `your-project.vercel.app` |
| 创建 Pull Request | Preview | `your-project-xxx.vercel.app` |
| 推送到其他分支 | Preview | `your-project-branch-xxx.vercel.app` |

### 修改默认分支

如果主分支不是 `main`：

1. 进入项目 **Settings** → **Git**
2. 修改 **Production Branch** 为实际主分支名称

---

## 🌐 自定义域名配置（可选）

### 添加域名

1. 进入项目 **Settings** → **Domains**
2. 输入域名，点击 **Add**

```
┌───────────────────────────────────────────────┐
│  Domains                                      │
│                                               │
│  ┌───────────────────────────────────────┐   │
│  │ admin.yourdomain.com      [Add]       │   │
│  └───────────────────────────────────────┘   │
└───────────────────────────────────────────────┘
```

### 配置 DNS

根据 Vercel 提示，在域名服务商处添加 DNS 记录：

**A 记录方式（推荐）：**
```
Type: A
Name: admin (或 @)
Value: 76.76.21.21
```

**CNAME 方式：**
```
Type: CNAME
Name: admin
Value: cname.vercel-dns.com
```

### SSL 证书

Vercel 自动配置免费 SSL 证书，无需手动操作。

---

## 🔧 常见问题排查

### 1. 构建失败：TypeScript 错误

**现象**：构建日志显示 TypeScript 类型错误

**当前配置**：已设置 `ignoreBuildErrors: true`，不会阻止构建

**长期建议**：逐步修复 TypeScript 错误，然后移除此配置

### 2. 构建失败：依赖安装错误

**现象**：`npm install` 或 `pnpm install` 失败

**解决方案**：
```bash
# 删除锁文件，重新生成
rm package-lock.json pnpm-lock.yaml
npm install
git add package-lock.json
git commit -m "Regenerate lock file"
git push
```

### 3. 运行时错误：环境变量未定义

**现象**：页面显示空白或出现 `undefined` 错误

**检查清单**：
- [ ] 确认环境变量名称拼写正确
- [ ] 确认变量已添加到正确的环境（Production/Preview）
- [ ] `NEXT_PUBLIC_` 前缀的变量在客户端可用
- [ ] 重新部署以应用新的环境变量

### 4. 认证失败：Supabase 连接问题

**现象**：登录页面无法完成认证

**检查清单**：
- [ ] `NEXT_PUBLIC_SUPABASE_URL` 配置正确
- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` 配置正确
- [ ] Supabase 项目处于活跃状态（非暂停）
- [ ] 检查 Supabase 认证提供商配置

### 5. 中间件重定向循环

**现象**：页面无限重定向，显示 "Too many redirects"

**当前配置**：中间件已排除 `/admin/login` 和 `/admin/403` 路径

**检查**：确认 [`middleware.ts`](middleware.ts:8) 中的 `publicPaths` 配置正确

### 6. 构建超时

**现象**：构建耗时过长被中断

**解决方案**：
1. 检查是否有未优化的大型静态资源
2. 考虑使用 `next/dynamic` 动态导入大型组件
3. 升级 Vercel 计划获取更长构建时间

### 7. Serverless 函数超时

**现象**：API 请求返回 504 超时错误

**解决方案**：
1. 优化数据库查询
2. 在 `vercel.json` 中增加函数超时时间（Pro 计划）
3. 考虑使用边缘函数

---

## 📝 部署后检查清单

部署完成后，验证以下功能：

- [ ] 首页正常加载
- [ ] 登录页面 `/admin/login` 可访问
- [ ] Supabase 认证正常工作
- [ ] 管理员仪表盘正常显示
- [ ] 内容管理功能正常
- [ ] 403 页面对未授权用户正常显示
- [ ] 响应式布局在移动设备正常

---

## 📚 相关资源

- [Vercel 官方文档](https://vercel.com/docs)
- [Next.js 部署文档](https://nextjs.org/docs/deployment)
- [Supabase + Vercel 集成指南](https://supabase.com/docs/guides/getting-started/quickstarts/nextjs)
- [Vercel 环境变量文档](https://vercel.com/docs/environment-variables)

---

## 🔄 更新历史

| 日期 | 更新内容 |
|------|----------|
| 2026-01-29 | 初始版本，完成项目分析和部署指南 |
